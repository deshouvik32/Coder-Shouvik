int stringLength(char[]);
main()
{
	char str[30];
	int len;
	printf("Enter a string:");
	gets(str);
	len=stringLength(str);
	printf("Length:%d\n",len);
	stringlength(charx[])
	
}
int stringLength(char[x])
{
	int x=0, count=0;
	while(x[i]!='\0')
	{
		count++;
		i++;
	}
}
